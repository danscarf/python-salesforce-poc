pipeline {
  agent {
    node {
      label 'docker'
    }

  }
  stages {
    stage('') {
      agent {
        node {
          label 'docker'
        }

      }
      steps {
        sh 'docker ps -a'
      }
    }
  }
}